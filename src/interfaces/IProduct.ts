export interface IProduct {
    id: number;
    isFavorite: boolean;
    article: string;
    title: string;
    author: string;
    year: string;
    coverPath: string;
    description: string;
}
