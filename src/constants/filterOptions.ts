type FilterOptionsProps = {
    isFavorites: string;
    isNotFavorites: string;
    all: string;
}

export const filterOptions: FilterOptionsProps = {
    isFavorites: "Favorites",
    isNotFavorites: "Not favorites",
    all: "All",
};
