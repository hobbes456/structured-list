const ERROR404_MESSAGE: string =
    "(This page does not exist, to return to the home page click on the link above)";

const NETWORK_ERROR_MESSAGE: string =
    "(Unfortunately there was a network error, please check back later)";

export const errorMessages = {
    ERROR404_MESSAGE,
    NETWORK_ERROR_MESSAGE,
};
